# Text Classification Mode

Classify the current text request using the shared semantic contract and the supplied JSON schema.
Use supplied context candidates only when the request depends on prior conversation. Return only
the structured result, with no answer, prose, markdown, or chain-of-thought.
